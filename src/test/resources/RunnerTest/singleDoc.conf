# This is a configuration file managed by https://github.com/lightbend/config
# It is in HOCON format, a superset of JSON

connectors: [
    {class: "com.kmwllc.lucille.connector.CSVConnector", path: "classpath:RunnerTest/singleDoc.csv", name: "connector1", pipeline: "pipeline1"}
]

indexer {
  batchSize = 100
  batchTimeout = 100
}

pipelines: [{name: "pipeline1", stages: [{class: "com.kmwllc.lucille.stage.ExampleStage"}]}]
