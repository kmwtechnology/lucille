{
  class : "com.kmwllc.lucille.stage.QueryDatabase"
  fieldMapping : {
    "name" : "output1",
  }
  driver: "org.h2.Driver"
  sql: "SELECT name FROM meal WHERE id = ? AND animal_id = ?"
  jdbcUser: ""
  jdbcPassword: ""
  connectionString: "jdbc:h2:mem:test"
  keyField: "fish"

}