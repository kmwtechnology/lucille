{
  class = "com.kmwllc.lucille.stage.Concatenate"
  source = ["country", "state", "city"]
  dest = "dest"
  format_string = "{city}, {state}, {country}"
  default_inputs {
    "country" : "U.S."
    "state" : "MA"
  }
}