{
  text_field: "text"
  chunking_method: "sentence"
  # how many chunks to put in a buffer
  new_chunk_size: 3
  # how many chunks should of the previous buffer should be in the next buffer
  chunks_to_overlap:1
}