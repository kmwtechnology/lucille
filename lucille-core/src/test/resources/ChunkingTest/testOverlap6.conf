{
  text_field: "text"
  chunking_method: "sentence"
  # how many smaller chunks to merge into one final chunk
  final_chunk_size: 5
  # how many smaller chunks from one final chunk should be in the next final chunk
  chunks_to_overlap: 1
}