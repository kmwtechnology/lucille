{
  class = "com.kmwllc.lucille.stage.SetLookup"

  file_path = "classpath:SetLookupTest/missing.txt"
  source = "field"

  # optional parameters
  # destination = "setContains"
  # ignore_case = true
  # ignore_missing_source = true
}