kafka {
  producerPropertyFile: "classpath:KafkaUtilsTest/producer-conf/producer.properties"

  producer {
    # to HOCON, this is identical to writing ' security.protocol: "none" '
    # we don't / cannot prevent it, so we do want to make sure it is interpreted correctly.
    security {
      protocol: "none"
    }
  }
}
