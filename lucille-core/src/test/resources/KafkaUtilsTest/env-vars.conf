kafka {
  bootstrapServers: "localhost:9092"
  securityProtocol: "SSL"
  maxRequestSize: 250000000
  consumerGroupId: "something"
  maxPollIntervalSecs: 123

  producer {
    prop: ${PRODUCER_PROP}
  }

  consumer {
    prop: ${CONSUMER_PROP}
  }
}
