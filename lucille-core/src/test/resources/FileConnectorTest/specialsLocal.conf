{
  name: "file-connector-special"
  pipeline: "pipeline1"
  class: "com.kmwllc.lucille.connector.FileConnector"
  pathsToStorage: ["./src/test/resources/FileConnectorTest/directory%20with%20spaces"]

  filterOptions: {
    # excluding the macOS hidden files
    excludes: ["^(?:__MACOSX/|.*?/\\._.*|\\._.*).*$", ".*\\.DS_Store$"]
  }
}