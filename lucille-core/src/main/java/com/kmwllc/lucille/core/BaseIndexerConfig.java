package com.kmwllc.lucille.core;

/**
 * Base configuration class for indexers.
 * <p>
 * Extend this class to add indexer-specific configuration fields and logic.
 * Inherits common configuration functionality from {@link BaseConfig}.
 */
public class BaseIndexerConfig extends BaseConfig {
  // Add indexer-specific shared configuration fields or methods here if needed in the future.
}
