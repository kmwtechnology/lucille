connectors: [
  {
    class: "com.kmwllc.lucille.mongodb.connector.MongoDBConnector",
    name: "mongo_connector",
    pipeline: "simple_pipeline",
    uri: "mongodb://xx.xx.xx.xx:27017",
    database: "db",
    collection: "testcollection"
  }
]

pipelines: [
  {
    name: "simple_pipeline"
    stages: [
    ]
  }
]

indexer {
  type : "Elasticsearch"
  batchSize : 50
  batchTimeout : 1000
  logRate : 1000
}

elasticsearch {
  url: "https://user:password@localhost:9200"
  index: "myindex"
  type: "lucille-type"
  sendEnabled: true
  acceptInvalidCert: true
}
