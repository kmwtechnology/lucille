server:
  type: default
  requestLog:
    type: external
logging:
  type: external

presetConfig:
  configDirectoryPath: "src/test/resources/presets"

auth:
  type: "basicAuth"
  enabled: true
  password: "password"

swagger:
  resourcePackage: "com.kmwllc.lucille.endpoints"
  title: Lucille API
  description: Lucille API
  version: 0.0.1
  contact: info@kmwllc.com
  license: Apache 2.0
  securityDefinitions:
    basicAuth:
      type: "basic"
  security:
    - basicAuth: []      