# Sample workflow for building and deploying a Hugo site to GitHub Pages
name: Deploy Hugo site to Pages

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["main"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  # change contents to write access so we can add the version docs
  contents: write
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

# Default to bash
defaults:
  run:
    shell: bash

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    env:
      HUGO_VERSION: 0.155.3
    steps:
      - name: Install Hugo CLI
        run: |
          wget -O ${{ runner.temp }}/hugo.deb https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}/hugo_extended_${HUGO_VERSION}_linux-amd64.deb \
          && sudo dpkg -i ${{ runner.temp }}/hugo.deb
      - name: Install Dart Sass
        run: sudo snap install dart-sass
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0
      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v5
      - name: Install Node.js dependencies
        run: "[[ -f package-lock.json || -f npm-shrinkwrap.json ]] && npm ci || true"
        working-directory: doc/site
      - name: Install postcss
        run: npm install postcss-cli
        working-directory: doc/site

      - name: Generate versioned docs and update hugo.toml
        working-directory: doc/site
        env:
          BASE_URL: ${{ steps.pages.outputs.base_url }}
        run: |
          # Remove any version folders from previous runs in case there are any
          rm -rf content/en/docs-*
          # Restore hugo.toml in case it was somehow altered
          git checkout HEAD -- hugo.toml

          # Minimum version to expose on the docs site. Tags older than this are
          # skipped entirely, they get no docs-<tag> folder and no dropdown entry.
          min_version="0.5.10"

          # Helper which returns success if $1 a tag is at or above $min_version. Strips leading v for v0.2.4
          tag_at_least_min() {
            local t="${1#v}"
            [ "$(printf '%s\n%s\n' "$min_version" "$t" | sort -V | head -n1)" = "$min_version" ]
          }

          # Filtered list of tags at or above min_version
          tags=$(git tag --list | while IFS= read -r t; do
            tag_at_least_min "$t" && printf '%s\n' "$t"
          done)

          # Descending (newest-first) list used for the dropdown selector order
          tags_descending=$(echo "$tags" | sort -rV)

          # Get the latest tag; its docs will replace the working directory's docs/
          latest_tag=$(echo "$tags_descending" | grep -v '^v' | head -1)
          echo "Latest tag: $latest_tag"

          # Use "git archive" to extract docs from each tag into a temp directory, then
          # copy them into place. This avoids "git checkout" which would overwrite the
          # current docs folder and leave behind extra files from older tags.

          # IFS= means preserve any whitespace, read -r says to read the tag exactly as is (ignoring backslashes)
          while IFS= read -r tag; do
            echo "Processing tag: $tag"

            # Check if docs exist in this tag
            git ls-tree "$tag" content/en/docs >/dev/null 2>&1 || { echo "Skipping $tag - docs not found"; continue; }

            # Make a temporary directory for git archive, then archive the tag into it
            tmpdir=$(mktemp -d)
            git archive "$tag" content/en/docs | tar -x -C "$tmpdir"

            if [ "$tag" = "$latest_tag" ]; then
              # Latest tag replaces the main docs/ folder
              rm -rf content/en/docs
              cp -r "$tmpdir/content/en/docs" content/en/docs
            else
              # Older tags go into their own docs-{tag} folder
              cp -r "$tmpdir/content/en/docs" "content/en/docs-$tag"

              # Update _index.md to specify title and link title. (EOF marker needs to be at col 0)
              cat > "content/en/docs-$tag/_index.md" <<EOF
          ---
          title: Documentation $tag
          linkTitle: Docs-$tag
          weight: 20
          cascade:
            type: docs
            exclude_search: true
          ---
          EOF
            fi
            rm -rf "$tmpdir"
          done <<< "$tags"

          # Write the version params and versions block to a temp file.
          # Tags are listed in reverse version order (newest first) so the dropdown
          # reads top-to-bottom from newest to oldest.
          {
            printf 'version = "%s (latest)"\n' "$latest_tag"
            printf 'url_latest_version = "/docs/"\n'
            printf '\n'

            # Add the latest version first, it points to the main /docs/ path
            printf '[[params.versions]]\n'
            printf '  version = "%s (latest)"\n' "$latest_tag"
            printf '  url = "%s/docs/"\n' "$BASE_URL"
            printf '\n'

            while IFS= read -r tag; do
              # Skip the latest tag, it's already added above
              if [ "$tag" = "$latest_tag" ]; then continue; fi
              printf '[[params.versions]]\n'
              printf '  version = "%s"\n' "$tag"
              printf '  url = "%s/docs-%s/"\n' "$BASE_URL" "$tag"
              printf '\n'
            done <<< "$tags_descending"
          } > /tmp/versions_block.txt

          # Insert the versions block immediately before [params.ui] in hugo.toml.
          # [[params.versions]] is an array-of-tables, so any plain key/value pairs
          # appearing after it are parsed as properties of the last version entry
          # rather than of [params] itself (silently breaks offlineSearch,
          # algolia_docsearch, github_repo, etc.). Anchoring on [params.ui] puts
          # the block after all top-level [params] keys and right before the next
          # sub-table header. The first four rules strip any pre-existing
          # [[params.versions]] blocks so the step is idempotent regardless of
          # what cruft lives in the committed hugo.toml.
          awk '
            /^\[params\.ui\]/{while((getline line < "/tmp/versions_block.txt") > 0) print line; print ""; print; next}
            /^version = /{next}
            /^url_latest_version = /{next}
            {print}
          ' hugo.toml > hugo.toml.tmp && mv hugo.toml.tmp hugo.toml

      - name: Build with Hugo
        env:
          # For maximum backward compatibility with Hugo modules
          HUGO_ENVIRONMENT: production
          HUGO_ENV: production
        run: |
          hugo \
            --minify \
            --baseURL "${{ steps.pages.outputs.base_url }}/"
        working-directory: doc/site
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./doc/site/public

      - name: Remove content from docs folder and restore it
        run: |
          rm -rf content/en/docs-*
          git checkout HEAD -- content/en/docs

  # Deployment job
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
