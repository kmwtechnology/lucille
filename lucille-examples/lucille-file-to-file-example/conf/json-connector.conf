# this illustrates how the configuration for a connector can be placed in a separate file and referenced from elsewhere
class: "com.kmwllc.lucille.connector.FileConnector"
name: "json_connector"
pathsToStorage: ["conf/source.jsonl"]
pipeline: "simple_pipeline"
fileOptions: {
  json: { }
}